# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas
*.rs.bk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db

# Local tool state (not shared)
local-config/state.json

# Local-only dev artifacts (specs, mockups, README previews — for the
# project maintainer, not the public)
docs/artifacts/*.html

# Tauri updater signing keys (never commit private keys)
updater.key
updater.key.pub
*.key

# Lockfile noise from package manager swaps
pnpm-lock.yaml
